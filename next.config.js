/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Não usar i18n aqui, pois estamos usando middleware para internacionalização
  // Outras configurações do Next.js, se necessário
};

module.exports = nextConfig;
