// Este arquivo foi substituído por app/[locale]/layout.tsx
// Mantido apenas para compatibilidade

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
